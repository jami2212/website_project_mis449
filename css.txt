:root {
    --primary: #3498DB;
    --success: #2ECC71;
    --warning: #F39C12;
    --danger: #E74C3C;
    --bg: #F4F7F9;
    --text: #2C3E50;
    --card-bg: #FFFFFF;
}
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: var(--bg);
    color: var(--text);
    margin: 0;
    padding: 20px;
    display: flex;
    justify-content: center;
}
.app-container {
    width: 100%;
    max-width: 460px;
    background: var(--card-bg);
    border-radius: 16px;
    padding: 20px;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
    box-sizing: border-box;
}
h1 {
    text-align: center;
    color: var(--primary);
    font-size: 22px;
    margin-top: 0;
}
h3 {
    font-size: 16px;
    margin-bottom: 10px;
    color: #555;
}
.history-sub {
    font-size: 11px;
    font-weight: normal;
    color: #A0AEC0;
    text-transform: none;
}
.burnout-banner {
    background: #EBF5FB;
    border-left: 5px solid var(--success);
    padding: 12px;
    border-radius: 8px;
    margin-bottom: 20px;
    transition: background 0.3s ease, border-color 0.3s ease;
}
.burnout-header {
    display: flex;
    justify-content: space-between;
    font-weight: bold;
    font-size: 13px;
}
#burnout-msg {
    margin: 6px 0 10px 0;
    font-size: 12px;
    color: #555;
}
.status-low { color: var(--success); }
.status-med { color: var(--warning); }
.status-high { color: var(--danger); }
.break-btn {
    background-color: var(--warning);
    color: white;
    border: none;
    padding: 6px 12px;
    border-radius: 6px;
    cursor: pointer;
    font-size: 12px;
    font-weight: 600;
}
.metrics-section {
    margin-bottom: 20px;
}
.metrics-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
}
.metric-card {
    background: #F8FAFC;
    padding: 10px;
    border-radius: 10px;
    text-align: center;
    border: 1px solid #E2E8F0;
}
.metric-value {
    display: block;
    font-size: 18px;
    font-weight: bold;
    color: var(--text);
}
.metric-label {
    font-size: 11px;
    color: #718096;
    text-transform: uppercase;
    font-weight: 600;
}
.task-input-card {
    display: flex;
    gap: 8px;
    margin-bottom: 20px;
}
.task-input-card input[type="text"] {
    flex: 2;
    padding: 10px;
    border: 1px solid #CBD5E0;
    border-radius: 8px;
    font-size: 14px;
    min-width: 0;
}
.task-input-card input[type="number"] {
    flex: 1;
    padding: 10px;
    border: 1px solid #CBD5E0;
    border-radius: 8px;
    font-size: 14px;
    min-width: 0;
}
.task-input-card button {
    background-color: var(--primary);
    color: white;
    border: none;
    padding: 10px 16px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 600;
    white-space: nowrap;
}
ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
}
.task-item {
    background: #F8FAFC;
    margin-bottom: 10px;
    padding: 12px;
    border-radius: 10px;
    border: 1px solid #E2E8F0;
    display: flex;
    flex-direction: column;
    gap: 8px;
}
.task-item.active {
    border-color: var(--primary);
    background: #EBF5FB;
}
.task-item.over-estimate {
    border-color: var(--danger);
}
.task-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
}
.task-title {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 600;
}
.task-title.completed span {
    text-decoration: line-through;
    color: #A0AEC0;
}
.task-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 13px;
    flex-wrap: wrap;
    gap: 6px;
}
.time-spent {
    font-weight: bold;
    color: var(--primary);
}
.time-spent.over {
    color: var(--danger);
}
.estimate-label {
    font-size: 11px;
    color: #A0AEC0;
    font-weight: normal;
}
.task-actions button {
    border: none;
    padding: 5px 10px;
    border-radius: 6px;
    cursor: pointer;
    font-size: 12px;
    margin-left: 4px;
    font-weight: 600;
}
.btn-start { background-color: var(--success); color: white; }
.btn-pause { background-color: var(--warning); color: white; }
.btn-delete { background-color: var(--danger); color: white; }

.history-section {
    margin-top: 24px;
    border-top: 1px solid #E2E8F0;
    padding-top: 16px;
}
.history-item {
    display: flex;
    justify-content: space-between;
    padding: 8px 10px;
    background: #F8FAFC;
    border-radius: 8px;
    margin-bottom: 6px;
    font-size: 12px;
    border: 1px solid #E2E8F0;
}
.history-date {
    font-weight: 600;
    color: #555;
}
.history-stats {
    color: #718096;
}
.history-empty {
    font-size: 12px;
    color: #A0AEC0;
    text-align: center;
    padding: 10px 0;
}